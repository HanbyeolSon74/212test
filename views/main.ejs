<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <title>Document</title>
  </head>
  <body>
    <button onclick="testOnclick()">요청</button>
  </body>
</html>
<script>
  const data = {
    id: 12,
    name: "rlaasdasd",
    age: 123,
  };
  const testOnclick = () => {
    // axios({
    //   method: "get",
    //   url: "/axiosget",
    //   params: data,
    // }).then((res) => {
    //   console.log(res, "asd");
    // });

    axios({
      method: "post",
      url: "/axiospost",
      data: data,
    }).then((res) => {
      console.log(res, "asd");
    });
  };

  const pageMove = (page) => {
    axios({
      method: "get",
      url: "/page",
    }).then((res) => {});
  };
</script>
